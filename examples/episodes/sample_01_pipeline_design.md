# Pipeline design conversation

Director: I want a knowledge graph layer over my conversations, but Graphiti
edges only carry what connects two nodes — not why I drew that line. The why
is where the user's actual cognition lives. We need a second pass that
annotates each edge with the intent behind it.