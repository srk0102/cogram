# Validation test

Director: The test of whether the annotation layer works is brutal: hand a
fresh model the Director profile JSON with no other context, ask it about my
working style, and see if its answer matches reality. If it can describe how
I think without ever seeing the conversations directly, the layer captured
something real. If it produces a generic description, we failed.