# Cogram Redis config — active subgraph hot tier
# Keep memory bounded; LRU evicts cold subgraph nodes automatically.

maxmemory 1gb
maxmemory-policy allkeys-lru
save ""
appendonly no

# Allow inbound connections from other containers on cogram network
bind 0.0.0.0
protected-mode no
